[/Script/EngineSettings.GeneralProjectSettings]
ProjectID=F1BAF0474C1E193855A14A88348430A1
ProjectName=VCORE

[AGVSim]
BackendHost=localhost
BackendPort=8000
APIKey=dev-agv-key
; Real-time telemetry fan-out to the telemetry-collector service.
; ws  = stream over the backend WebSocket; the backend forwards to the collector
;       in-Docker. Required on Windows: UE5 raw-socket udp/tcp does not traverse
;       Docker Desktop's proxy reliably (it connects but bytes are dropped).
; tcp/udp/both = direct raw-socket emit (works off Docker Desktop Windows).
TelemetryHost=127.0.0.1
TelemetryTransport=ws
TelemetryUdpPort=9999
TelemetryTcpPort=9998
CellId=cell_demo

