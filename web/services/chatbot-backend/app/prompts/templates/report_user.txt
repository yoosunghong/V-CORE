Event:
$event_context

Robot command:
$command_context

AI evaluation draft (combine into the narrative; "none" means no run metrics to evaluate):
$evaluation_context

참고 문서 (지식 베이스에서 검색됨; "none"이면 관련 문서 없음):
$knowledge_context

Write the final report. When 참고 문서 are provided, ground the assessment in them and cite the
relevant document title(s) inline (e.g. "[출처: …]"). Do not invent facts not in the documents.
